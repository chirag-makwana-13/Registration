<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <title>Exam </title>
    <style>
        body {
            background-color: rgb(185, 218, 250);
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px;
        }

        button {
            height: 50px;
            border: none;
            background-color: rgb(185, 218, 250);
            font-size: 20px;
            padding: 15px 15px;

        }

        table,
        th,
        td {
            border: 1px solid black;
            background-color: rgb(217, 215, 250);
            border-collapse: collapse;
            text-align: center;
            padding: 10px;
            margin-left: 250PX;
        }

        h1,
        h3 {
            text-align: center;
        }

        a {
            text-decoration: none;
        }

        div {
            display: flex;
            justify-content: center;
            margin: 30px;
        }

        button {
            height: 20px;
        }

        h5 {
            margin-top: 5px;
        }

        .b1 {
            font-size: 20px;
            background-color: rgb(133, 183, 233);
        }

        #months {
            height: 40px;
            background-color: rgb(0, 4, 63);
            color: rgb(185, 218, 250);
            font-size: 20px;
            border-radius: 10px;
            border: none;
            text-align: center;
        }
    </style>
</head>

<body>
    <h1>Chirag Makwana <br>( ID:24DEV095 )</h1>
    <h1><a href="/attendance/allstudent/1">Attendence 3 months Here</a></h1>
    <table>
        <tr>
            <th colspan="2" ></th>
            <th colspan="2">Prelim</th>
            <th colspan="2">Terminal</th>
            <th colspan="2">Final</th>
        </tr>
       
        <% for(let val of data){%>
            <%const fieldnames=Object.keys(val);%>
                <tr>
                    <%for(var i=0;i<fieldnames.length;i++){%>
                        <th>
                            <%=fieldnames[i]%>

                        </th>
                        <%}%>
                        <th>Report</th>
                </tr>
                <%break;%>
                    <%}%>
                        <% for(let key of data){%>
                            <%const fieldValues=Object.values(key);%>
                                <tr>
                                    <%for(var i=0 ;i<fieldValues.length;i++){%>
                                        <td>
                                            <%=fieldValues[i]%>

                                        </td>
                                        <%}%>
                                        <td><a href="/attendance/report/<%=fieldValues[0]%>">View data</a></td>
                                </tr>
                                <%}%>
    </table>
</body>
</html>